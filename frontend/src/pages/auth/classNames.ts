export const CLASS_PREFIX = "auth";

export const classNames = {
  page: `${CLASS_PREFIX}-page`,
  form: `${CLASS_PREFIX}-form`,
  field: `${CLASS_PREFIX}-form-field`,
  error: `${CLASS_PREFIX}-form-error`,
  actions: `${CLASS_PREFIX}-form-actions`,
  switch: `${CLASS_PREFIX}-switch`,
};
