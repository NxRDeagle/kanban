export const CLASS_PREFIX = "board-card";

export const classNames = {
  sortable: `${CLASS_PREFIX}-sortable`,
  root: CLASS_PREFIX,
  overlay: `${CLASS_PREFIX}-overlay`,
  header: `${CLASS_PREFIX}-header`,
  title: `${CLASS_PREFIX}-title`,
  actions: `${CLASS_PREFIX}-actions`,
  description: `${CLASS_PREFIX}-description`,
};
