export const CLASS_PREFIX = "task-card";

export const classNames = {
  root: CLASS_PREFIX,
  overlay: `${CLASS_PREFIX}-overlay`,
  title: `${CLASS_PREFIX}-title`,
  description: `${CLASS_PREFIX}-description`,
};
