export const CLASS_PREFIX = "column";

export const classNames = {
  root: CLASS_PREFIX,
  header: `${CLASS_PREFIX}-header`,
  dragHandle: `${CLASS_PREFIX}-drag-handle`,
  title: `${CLASS_PREFIX}-title`,
  actions: `${CLASS_PREFIX}-actions`,
  overlay: `${CLASS_PREFIX}-overlay`,
  dragging: `${CLASS_PREFIX}-dragging`,
  tasks: `${CLASS_PREFIX}-tasks`,
  tasksOver: `${CLASS_PREFIX}-tasks-over`,
  addTaskButton: "add-task-button",
};
