export const CLASS_PREFIX = "app";

export const classNames = {
  layout: `${CLASS_PREFIX}-layout`,
  header: `${CLASS_PREFIX}-header`,
  title: `${CLASS_PREFIX}-title`,
  headerActions: `${CLASS_PREFIX}-header-actions`,
  username: `${CLASS_PREFIX}-username`,
  main: `${CLASS_PREFIX}-main`,
};
